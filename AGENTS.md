# Runtime Setup
- Before running project commands, execute `source ~/.nvm/nvm.sh && nvm use 24.15.0`.
- If `nvm use 24.15.0` fails, run `source ~/.nvm/nvm.sh && nvm install 24.15.0 && nvm use 24.15.0`.
